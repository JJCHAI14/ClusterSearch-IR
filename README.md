# ClusterSearch-IR
A high-efficiency Information Retrieval engine utilizing concurrent web crawling, TF-IDF representations, and a custom K-Means clustering pipeline to accelerate search query speeds by 4-5x.
